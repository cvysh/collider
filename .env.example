# API port. Deliberately not 8000, which collides with most other dev servers.
COLLIDER_API_PORT=8123
COLLIDER_API_BASE=http://127.0.0.1:8123

# Overrides for the data pipeline; defaults are relative to the repo root.
# COLLIDER_EVENTS_ROOT=data/processed/events
# COLLIDER_REGISTRY=ml/models/registry.json
